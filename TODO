+--------+
| URGENT |
+--------+

- unifier système de build :
  + dans makefile principal, récupérer variables définies sur la ligne de commande et mettre valeur par défaut à celles indéfinies
  + faire règles suivantes dans makefile principal
    + system
    + installer
    + applications
    + libraries
  + les divers sous-systèmes ne doivent plus pouvoir compiler seuls
  + make all a le même fonctionnement que maintenant
  + les variables de compilation sont visibles via la commande version du système

Système
-------

- même variable CONFIGURATION_IS_DEBUG_ENABLED dans MBR ?
- choix manuel LBA28 - LBA48 pour coller au design du système

Doc
---
Rajouter dans doc la liste des programmes nécessaires pour compiler l'OS
- genisoimage (aka mkisofs)
- hexdump (sera utilisé peut-être par l'installateur)*
- dd (installé de base sur la plupart des systèmes)
- ar (//)
- gcc / ld
- nasm
- ...
- qfsm pour édition machines d'état documentation

Unifier tous les fichiers différents dans "Description du système"

mettre à jour manuel d'installation du système

Libraries
---------

modif bibliothèques pour gérer nouveaux codes touches

- bibliothèque userspace qui gère les fenêtres en ASCII art en exploitant le syscall de copie de buffer vidéo

- mettre Lemon devant tous les noms de fonctions publics + constantes...

- utiliser les fonctions de conversion de string pour les fonctions screenwriteint...

Applications
------------

- hexdump ?
- optimiser système de build

- utiliser api buffer pour affichage éditeur de texte

Installer
---------

- mettre .h dans Includes
